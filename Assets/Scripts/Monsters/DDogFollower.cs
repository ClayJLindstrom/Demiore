using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//the followers
// - Get a path from the leader to go, (Essentially follows the leader where it goes, until close
//to the player, then attack! If needed, we will teleport the dog back to the leader.
// - retreat to the leader when called.
public class DDogFollower : DemiDogScript
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
